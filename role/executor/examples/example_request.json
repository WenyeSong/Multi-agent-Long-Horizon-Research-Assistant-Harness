{
  "request_id": "exec_001",
  "planner_id": "planner_run_001",
  "task": {
    "title": "Toy dataset summary run",
    "objective": "Read the provided toy dataset, compute summary metrics for the configured target column, and produce a diagnostic figure.",
    "scope": "Use only the provided baseline config and dataset. Generate numerical metrics, a summary table, and a diagnostic figure.",
    "non_goals": [
      "Do not search for alternative algorithms.",
      "Do not rewrite the global research plan.",
      "Do not generate final coursework code or a final report."
    ]
  },
  "workspace": {
    "root": "role/executor/workspaces/exec_001",
    "read_paths": [
      "role/executor/examples/inputs/baseline_config.json",
      "role/executor/examples/inputs/dataset.csv"
    ],
    "write_path": "role/executor/workspaces/exec_001"
  },
  "inputs": [
    {
      "artifact_id": "baseline_optimizer",
      "path": "role/executor/examples/inputs/baseline_config.json",
      "kind": "config",
      "description": "Toy baseline configuration for the self-contained executor example.",
      "read_only": true
    },
    {
      "artifact_id": "dataset_main",
      "path": "role/executor/examples/inputs/dataset.csv",
      "kind": "dataset",
      "description": "Toy dataset for the self-contained executor example.",
      "read_only": true
    }
  ],
  "success_criteria": [
    {
      "criterion_id": "crit_results_json",
      "description": "Produce results.json containing baseline metrics, attempted metrics, and the best observed value.",
      "check_type": "property_check",
      "target": {
        "required_keys": ["baseline", "attempts", "best_attempt"]
      },
      "required": true
    },
    {
      "criterion_id": "crit_metric_threshold",
      "description": "At least one attempted configuration must improve the baseline objective metric.",
      "check_type": "metric_threshold",
      "target": {
        "metric": "objective_value",
        "comparison": "greater_than_baseline"
      },
      "required": true
    },
    {
      "criterion_id": "crit_diagnostic_figure",
      "description": "Produce at least one diagnostic plot under figures/.",
      "check_type": "visual_inspection",
      "target": {
        "minimum_figures": 1
      },
      "required": true
    }
  ],
  "budget": {
    "max_attempts": 3,
    "max_wall_time_minutes": 20,
    "max_compute_notes": "Keep the sweep small enough for local execution."
  },
  "required_outputs": [
    "results.json",
    "tests.json",
    "environment.json",
    "figures",
    "logs",
    "notes.md"
  ],
  "tool_policy_ref": "role/executor/docs/tool_policy.md"
}
